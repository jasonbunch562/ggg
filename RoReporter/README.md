# RoReporter

This folder is initialized for the **RoReporter** project and now includes a practical starter layout.

## Current files
- `.gitkeep` keeps the folder tracked in git.
- `README.md` explains the project status and next steps.

## Upgrade + debug notes
- ✅ Fixed a navigation bug in `docs/index.html`: the **Launch Demo** button pointed to `demo.html`, which did not exist.
- ✅ Added `docs/demo.html` as a working placeholder demo page so the button no longer 404s.

## Recommended new additions
- Add a real app scaffold (for example `src/`, `tests/`, and a package/build manifest).
- Add linting + formatting (`prettier`, `eslint`, or language-specific tools).
- Add CI checks for unit tests and static analysis.
- Add issue/PR templates to standardize future contributions.
